## Telegram Bot — гид по Стамбулу

Бот-гид по Стамбулу с поддержкой:
- **Маршрутов** (1 / 2 / 3 дня, в том числе «лайтовый» вариант)
- **Достопримечательностей** по районам
- **Ресторанов** по районам
- **Интеграции с ИИ** (OpenRouter / OpenAI API)
- **Погоды для любого города мира** (OpenWeatherMap API с автоматической нормализацией названий)

## Структура проекта
- **`bot.py`**: основной файл бота (логика маршрутов, команд, кнопок, работы с ИИ и погодой)
- **`config.py`**: конфигурация и переменные окружения
- **`istanbul_db_v2.yaml`**: база данных маршрутов, мест и ресторанов (приоритетный формат v2)
- **`istanbul_guide.yaml`**: база данных маршрутов, мест и ресторанов (legacy формат, используется как fallback)
- **`requirements.txt`**: зависимости проекта
- **`sync.sh`**: скрипт для синхронизации и деплоя на сервер Selectel

## Технические детали

### Зависимости
- **`python-telegram-bot==20.3`**: библиотека для работы с Telegram Bot API (async)
- **`openai>=1.0`**: клиент для OpenRouter/OpenAI API
- **`python-dotenv>=1.0`**: загрузка переменных окружения из `.env`
- **`pyyaml`**: парсинг YAML-файлов с данными
- **`aiohttp>=3.9.0`**: асинхронный HTTP-клиент для запросов к API погоды
- **`pymorphy2>=0.9.1`**: морфологический анализатор для автоматической нормализации названий русских городов

### Архитектура
- **Асинхронная обработка**: все HTTP-запросы (погода, OpenRouter) выполняются асинхронно через `aiohttp`
- **Нормализация городов**: автоматическое приведение русских названий к именительному падежу через `pymorphy2`
- **История диалогов**: хранение последних 10 сообщений на пользователя для контекста ИИ
- **Умное определение промптов**: разные промпты для запросов про Стамбул, маршруты и общие вопросы
- **База данных**: поддержка двух форматов YAML (v2 и legacy) с автоматической адаптацией и валидацией
- **State machine**: управление состояниями диалога для кнопочных сценариев
- **Строгая работа из базы**: кнопочные сценарии (маршруты, достопримечательности, рестораны) работают строго из базы данных без LLM
- **Политика "никаких фактов из головы"**: LLM используется только для форматирования данных из базы, не для генерации новых фактов

## Сервер Selectel

- **Подключение к серверу**:
  ```bash
  ssh selectel
  ```
- **Путь к боту на сервере**: `/var/www/telegram_bot`

После входа на сервер:
```bash
cd /var/www/telegram_bot
```

## Работа с ботом: локальная разработка → деплой на сервер

### 1. Локальная разработка

- **Клонировать/обновить репозиторий локально**
- Убедиться, что в `.env` заданы необходимые ключи (локально и отдельно на сервере):
  - **`TELEGRAM_BOT_TOKEN`**
  - **`OPENROUTER_API_KEY`**
  - **`OPENWEATHER_API_KEY`**

### 2. Синхронизация с сервером (скрипт `sync.sh`)

Скрипт запускается локально, из директории проекта.

```bash
# Синхронизировать файлы с сервера на локальную машину
./sync.sh from

# Синхронизировать файлы с локальной машины на сервер
./sync.sh to

# Перезапустить бота на сервере (без синхронизации)
./sync.sh restart

# Полный деплой: синхронизировать файлы и перезапустить бота
./sync.sh deploy
```

Под капотом:
- используется `rsync` между локальной директорией `/Users/sergejsaburkin/projects/telegram_bot` и серверной `/var/www/telegram_bot`
- автоматически исключаются из синхронизации: `.git/`, `venv/`, `.env`, `*.log`, `bot.pid`
- **автоматическая установка зависимостей**: при синхронизации (`to` или `deploy`) автоматически выполняется `pip install -r requirements.txt` на сервере
- перезапуск бота на сервере выполняется через:
  - остановку процесса по `bot.pid` (если есть)
  - автоматический поиск Python в `venv/bin/` (поддерживает `python`, `python3`, `python3.10` и т.д.)
  - запуск бота в фоне с выводом в `bot.log`

**Дополнительные команды:**
```bash
# Очистка ненужных файлов на сервере (удаляет .git и другие исключённые файлы)
./sync.sh cleanup
```

### 3. Рекомендуемый сценарий обновления бота

- **После изменений локально**:
  ```bash
  # из локальной директории проекта
  ./sync.sh deploy
  ```
  Это:
  - отправит все изменения на сервер
  - перезапустит бота на сервере

## Проверка работы бота на сервере

Запускать команды можно как напрямую, так и через `ssh selectel '…'`.

```bash
# Просмотр логов бота в реальном времени
ssh selectel 'tail -f /var/www/telegram_bot/bot.log'

# Проверка, что процесс бота запущен
ssh selectel 'ps aux | grep bot.py'
```

При необходимости можно зайти внутрь и управлять руками:

```bash
ssh selectel
cd /var/www/telegram_bot
```

## Важные замечания

- **`.env` не синхронизируется**: ключи должны быть настроены отдельно на сервере и локально.
- **`.git/` не синхронизируется**: репозиторий Git не копируется на сервер (используй `./sync.sh cleanup` для удаления, если был скопирован ранее).
- При запуске `./sync.sh deploy` **бот автоматически перезапускается**.
- Логи бота пишутся в файл **`bot.log`** в директории `/var/www/telegram_bot` на сервере.
- **Systemd сервис**: на сервере может быть настроен `telegram_bot.service` для автозапуска. Для управления через `sync.sh` его нужно остановить: `systemctl stop telegram_bot.service`.

## Краткий чек-лист по деплою

- **Локально**:
  - внесены изменения в код
  - зависимости при необходимости добавлены в `requirements.txt`
- **Деплой**:
  ```bash
  ./sync.sh deploy
  ```
- **Проверка на сервере**:
  ```bash
  ssh selectel 'tail -n 50 /var/www/telegram_bot/bot.log'
  ```  

## Команды и кнопки бота (для пользователя Telegram)

- **Команды:**
  - **`/start`**: запуск бота, приветствие и показ основного меню с кнопками.
  - **`/help`**: краткая справка по доступным командам.
  - **`/route`**: маршрут на 1 день по Стамбулу (берётся из `istanbul_guide.yaml`).
  - **`/sights <район>`**: список достопримечательностей в заданном районе.  
    Примеры: `/sights Султанахмет`, `/sights Галата`.
  - **`/eat <район>`**: список ресторанов в заданном районе.  
    Примеры: `/eat Каракёй`, `/eat Бейоглу`.

- **Кнопки основного меню (ReplyKeyboard):**
  - **`🗺 Маршруты`**: открывает inline‑меню выбора маршрута (**1 / 2 / 3 дня**). Маршруты берутся строго из базы данных.
  - **`🏛 Достопримечательности`**: открывает inline‑меню с выбором района. Список достопримечательностей формируется строго из базы данных.
  - **`🍽 Рестораны`**: открывает inline‑меню с выбором района. Список ресторанов формируется строго из базы данных.
  - **`ℹ️ Помощь`**: выводит справку (аналог `/help`).
  - **`📄 Новый диалог`**: очищает историю диалога с ИИ для текущего пользователя и сбрасывает состояния.
  - **`🌍 Погода`**: показывает подсказку, как запросить погоду для любого города мира.

- **Погода:**
  - Бот понимает естественные запросы вида:  
    - «Какая **погода в Стамбуле**?»  
    - «Дай **прогноз погоды на 3 дня в Москве**».  
    - «**Температура в Нижнем Новгороде**».  
  - Ищутся ключевые слова **«погода» / «температура» / «прогноз»** + название города.
  - **Автоматическая нормализация**: бот автоматически приводит названия русских городов к именительному падежу (используется `pymorphy2`).
  - Поддерживаются города в любом падеже: «в Москве» → «Москва», «в Нижнем Новгороде» → «Нижний Новгород».
  - Работает для городов по всему миру (через OpenWeatherMap API).

- **Ответы ИИ:**
  - Любые произвольные вопросы отправляются в модель ИИ через OpenRouter.
  - **Важно**: кнопочные сценарии (маршруты, достопримечательности, рестораны) работают строго из базы данных без использования LLM.
  - Для свободных вопросов о Стамбуле бот сначала ищет релевантные данные в базе, затем передаёт их LLM только для форматирования (политика "никаких фактов из головы").
  - История переписки по пользователю хранится (последние несколько сообщений), контекст диалога учитывается.

## Мини‑FAQ

- **Изменения в коде не видны в Telegram‑боте**  
  - Убедись, что был запущен деплой:  
    ```bash
    ./sync.sh deploy
    ```  
  - Проверь, что команда отработала без ошибок, а на сервере обновился код.

- **Бот не отвечает или сразу падает**  
  - Посмотри последние строки лога на сервере:  
    ```bash
    ssh selectel 'tail -n 50 /var/www/telegram_bot/bot.log'
    ```  
  - Частые причины: не настроен `.env` на сервере или неверные ключи API.

- **Нужно обновить только маршруты/справочные данные**  
  - Отредактируй локально `istanbul_db_v2.yaml` (приоритетный) или `istanbul_guide.yaml` (legacy).  
  - Выполни деплой:  
    ```bash
    ./sync.sh deploy
    ```  
  - Бот перезапустится и подхватит новые данные. Бот автоматически использует `istanbul_db_v2.yaml`, если он существует, иначе fallback на `istanbul_guide.yaml`.

- **Добавлена новая зависимость в `requirements.txt`**  
  - Просто добавь зависимость в `requirements.txt` локально и установи её локально для тестирования:  
    ```bash
    pip install <новая_зависимость>
    ```  
  - При следующем деплое (`./sync.sh deploy`) зависимости автоматически установятся на сервере.  
  - **Важно**: убедись, что `requirements.txt` содержит актуальную версию зависимости.

- **Конфликт 409: "only one bot instance is running"**  
  - Это означает, что запущено несколько экземпляров бота одновременно.  
  - Решение:  
    ```bash
    ssh selectel
    cd /var/www/telegram_bot
    pkill -f "bot.py"  # остановить все процессы
    systemctl stop telegram_bot.service  # если есть systemd сервис
    ./sync.sh restart  # или запустить через sync.sh
    ```

## Инструменты разработки и деплоя

### Используемые технологии
- **Python 3.10+**: основной язык программирования
- **python-telegram-bot 20.3**: асинхронная библиотека для Telegram Bot API
- **OpenRouter API**: доступ к GPT-моделям (GPT-3.5-turbo)
- **OpenWeatherMap API**: данные о погоде для городов мира
- **aiohttp**: асинхронный HTTP-клиент для неблокирующих запросов
- **pymorphy2**: морфологический анализатор для автоматической нормализации русских названий городов
- **YAML**: структурированное хранение данных о маршрутах и местах

### Инфраструктура
- **Selectel VPS**: хостинг-провайдер для размещения бота
- **SSH**: удалённый доступ к серверу
- **rsync**: синхронизация файлов между локальной машиной и сервером
- **systemd**: управление сервисами на сервере (опционально)
- **Git**: система контроля версий (репозиторий на GitHub)

### Процесс разработки
1. **Локальная разработка** (macOS)
2. **Синхронизация** через `rsync` по SSH (`sync.sh`)
3. **Запуск на сервере** через `nohup` в фоновом режиме
4. **Логирование** в `bot.log`
5. **Управление процессом** через PID-файл (`bot.pid`) 